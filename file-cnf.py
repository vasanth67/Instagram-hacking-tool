def main():
    # Log in to Instagram
    login("your_username", "your_password")

    # Clone the specified Instagram account
    clone_account("account_to_clone")

    # Keep the browser window open until the user closes it manually
    while True:
        pass